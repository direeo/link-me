# ===========================================
# LinkMe Environment Configuration  
# ===========================================
# Copy this file to .env.local and fill in the values

# DATABASE
# --------
# For local development (SQLite):
DATABASE_URL="file:./dev.db"

# For production (Turso - recommended for Vercel):
# 1. Create account at https://turso.tech (free tier available)
# 2. Create a database: turso db create linkme
# 3. Get URL: turso db show linkme --url
# 4. Get token: turso db tokens create linkme
# DATABASE_URL="libsql://your-database.turso.io"
# DATABASE_AUTH_TOKEN="your-token-here"

# AUTHENTICATION
# --------------
# Generate secure random strings (at least 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-in-production"

# YouTube API (required for tutorial search)
# ------------------------------------------
# Get your API key from https://console.cloud.google.com
# Enable "YouTube Data API v3"
YOUTUBE_API_KEY="your-youtube-api-key"

# YouTube OAuth (optional - for playlist export feature)
# ------------------------------------------------------
# Create OAuth 2.0 credentials at https://console.cloud.google.com
# Add redirect URI: http://localhost:3000/api/youtube/callback
YOUTUBE_CLIENT_ID="your-youtube-client-id"
YOUTUBE_CLIENT_SECRET="your-youtube-client-secret"
YOUTUBE_REDIRECT_URI="http://localhost:3000/api/youtube/callback"

# Gemini AI API (required for learning path generation)
# -----------------------------------------------------
# Get your API key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key"

# EMAIL (optional for development)
# --------------------------------
# In development, emails are logged to console
# For production, configure SMTP:
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="LinkMe <noreply@linkme.com>"

# APPLICATION
# -----------
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ADMIN (for viewing users - optional)
# ------------------------------------
# Set a secret key to access /api/admin/users
ADMIN_SECRET="your-admin-secret-key"

# TWO-FACTOR AUTHENTICATION (optional)
# ------------------------------------
# Encryption key for 2FA secrets
TWO_FACTOR_ENCRYPTION_KEY="your-2fa-encryption-key"
